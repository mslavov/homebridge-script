{
  "name": "homebridge-scriptable-widget",
  "version": "1.0.0",
  "description": "TypeScript Scriptable widget for monitoring Homebridge",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "build:watch": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/scriptable-ios": "^1.7.8",
    "dotenv": "^16.3.1",
    "esbuild": "^0.19.8",
    "typescript": "^5.3.2"
  }
}
